import { View, Text } from 'react-native'
import * as React from 'react'
import { themeColors } from '../theme'

export default function OrchidCard() {
  return (
    <View style={{
        borderRadius: 40,
        backgroundColor: themeColors.bgLight,
        height: 300,
        width: 250,

    }}>
    </View>
  )
}